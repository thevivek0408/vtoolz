export default class WordEngine {
    constructor(canvas, config) {
        this.ctx = canvas.getContext('2d');
        this.ctx.fillStyle = 'white';
        this.ctx.fillText("Word Engine Logic Not Implemented Yet", 50, 50);
    }
    start() { }
    bindMobileControls() { }
}
