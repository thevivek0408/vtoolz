export default class CardEngine {
    constructor(canvas, config) {
        this.ctx = canvas.getContext('2d');
        this.ctx.fillStyle = 'white';
        this.ctx.fillText("Card Engine Logic Not Implemented Yet", 50, 50);
    }
    start() { }
    bindMobileControls() { }
}
