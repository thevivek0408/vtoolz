export default class IdleEngine {
    constructor(canvas, config) {
        this.ctx = canvas.getContext('2d');
        this.ctx.fillStyle = 'white';
        this.ctx.fillText("Idle/Clicker Engine Logic Not Implemented Yet", 50, 50);
    }
    start() { }
    bindMobileControls() { }
}
